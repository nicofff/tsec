class TseClient:
    def connect(self, endpoint):
        #Ready connection for data transfer
        # validate()
        #negotiateProtocol()
        return
    
    def validate (endPoint, identity):
        # Comunicate with trusted Endpoint/s and valdiate identity
        return
        
    def negociateProtocol(self):
        #Setup crypto protocol
        return
    
    def send(self, buffer):
        return
    
    def receive (self,  buffer):
        return
    

class TseServer:
    def listen(self):
        return
    
class Endpoint:
    def init(self, host, port):
        return
